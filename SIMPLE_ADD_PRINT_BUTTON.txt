// SIMPLE FIX: Add Print Button to Order Details Modal
// 
// In OrdersPage.tsx, find line 1240-1250 (Modal Header section)
// 
// FIND THIS CODE:
            <div className="bg-gradient-to-r from-red-700 to-red-900 text-white p-6 border-b border-red-600 flex justify-between items-center sticky top-0 z-20">
              <div>
                <h2 className="text-2xl font-bold">Order Details</h2>
                <p className="text-red-200 text-sm mt-1">
                  Order #{selectedOrder.order_number}
                </p>
              </div>
              <button
                onClick={handleCloseModal}
                className="text-white hover:text-red-200 transition-colors text-2xl"
              >
                ✕
              </button>
            </div>

// REPLACE WITH THIS:
            <div className="bg-gradient-to-r from-red-700 to-red-900 text-white p-6 border-b border-red-600 flex justify-between items-center sticky top-0 z-20">
              <div>
                <h2 className="text-2xl font-bold">Order Details</h2>
                <p className="text-red-200 text-sm mt-1">
                  Order #{selectedOrder.order_number}
                </p>
              </div>
              
              {/* Print and Close buttons */}
              <div className="flex items-center gap-3">
                <button
                  onClick={() => printBill(selectedOrder)}
                  className="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition-colors flex items-center gap-2"
                >
                  🖨️ Print
                </button>
                <button
                  onClick={handleCloseModal}
                  className="text-white hover:text-red-200 transition-colors text-2xl"
                >
                  ✕
                </button>
              </div>
            </div>

// That's it! The Print button will appear in the top-right corner of the modal
// next to the close (X) button.
//
// It will work for ALL order statuses (pending, processing, shipped, delivered)
